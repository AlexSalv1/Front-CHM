# Local dev can omit this because Vite proxies /api to localhost:8080.
# Production with Vercel rewrites can also leave it empty.
# If the front calls the API directly, set the full backend URL.
VITE_API_BASE_URL=
